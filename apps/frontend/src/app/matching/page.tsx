import PageClient from './PageClient';

export const metadata = {
  title: 'Finding your matches — Cleya.ai',
  description: 'Cleya is matching you with the right people in India\'s startup ecosystem.',
};

export default function MatchingPage() {
  return <PageClient />;
}
