You are a Senior Full-Stack Engineer. This is a Cleya.ai monorepo (apps/backend + apps/frontend, Next.js App Router + Prisma + PostgreSQL). You must fix ALL 6 bugs below using a strict FIX → TEST → VERIFY → LOOP cycle. Do NOT stop until every fix passes AND a full regression check confirms zero breakage.

=== LOOP PROTOCOL ===
REPEAT {
  1. Pick next unfixed bug
  2. Search the codebase for the exact files involved (use grep/find)
  3. Implement the fix
  4. Run the app, test the fix against the acceptance criteria
  5. PASS → mark DONE, move to next bug
  6. FAIL → debug, adjust, re-test
  7. After ALL 6 bugs DONE → run FULL REGRESSION CHECKLIST below
  8. Any regression found → fix it, re-run full checklist
} UNTIL all 6 fixes pass + full regression = ZERO failures

=== BUG 1: DELETE SAME-PERSONA MATCHES FROM DB (P0) ===
PROBLEM: Match records exist where both users are the same persona (e.g. Investor↔Investor). These should never exist.
FIND: Search for the matching function — grep -rn "prisma.match.create" apps/backend/
FIX STEPS:
  1. Create scripts/cleanup-matches.ts — use Prisma to find all Match records where user1.persona === user2.persona. Delete related Introduction records first (foreign key), then delete the Match.
  2. Run the script against the production database.
  3. Find the matching function (where new matches are generated). Add a filter so only CROSS-persona matches are created:
     - FOUNDER can match with INVESTOR or TALENT
     - INVESTOR can match with FOUNDER only
     - TALENT can match with FOUNDER only
  4. Add this filter: persona: { in: getMatchablePersonas(currentUser.persona) }
VERIFY: Run "SELECT count(*) FROM Match m JOIN User u1 ON m.user1Id=u1.id JOIN User u2 ON m.user2Id=u2.id WHERE u1.persona=u2.persona" — must return 0. Then trigger match generation for an Investor user and confirm NO Investor↔Investor matches appear.

=== BUG 2: REGENERATE ALL MATCH REASONING WITH AI (P0) ===
PROBLEM: Every match has the generic text "You both have complementary backgrounds and interests that could lead to a great connection." This must be replaced with AI-personalized reasoning.
FIND: grep -rn "complementary backgrounds" apps/ — find every file that sets this string.
FIX STEPS:
  1. First fix the SOURCE: wherever matches are created (prisma.match.create), replace the hardcoded reasoning string with an OpenAI call. The call should use gpt-4o-mini, pass both users' names/companies/industries/roles/stages, and generate exactly 2 specific sentences about why they should connect.
  2. Then fix EXISTING DATA: create scripts/regenerate-reasoning.ts that loops through ALL existing matches, fetches both users' profiles (FounderProfile/InvestorProfile/TalentProfile), calls OpenAI to generate personalized reasoning, and updates each match. Add a 500ms delay between calls to avoid rate limits.
  3. Run the script.
VERIFY: Query the database — SELECT reasoning FROM Match LIMIT 5. Every row must contain specific names/companies, NOT the generic placeholder. Also create a new match and confirm its reasoning is personalized.

=== BUG 3: FIX VIKRAM'S MISSING COMPANY NAME (P1) ===
PROBLEM: User vikram.patel.qa2026@gmail.com completed onboarding with company "DataMesh AI" but the Edit Profile page shows Company Name as empty.
FIND: 
  - Check the DB: SELECT fp."companyName" FROM "FounderProfile" fp JOIN "User" u ON fp."userId"=u.id WHERE u.email='vikram.patel.qa2026@gmail.com'
  - grep -rn "companyName" apps/frontend/ — find the profile edit form
  - grep -rn "companyName" apps/backend/ — find where onboarding saves it
FIX STEPS:
  1. If companyName is NULL in DB but was entered during onboarding: the onboarding chat extraction is not mapping the company name to the FounderProfile.companyName field. Find the onboarding save logic and ensure companyName is included in both the create and update operations of prisma.founderProfile.upsert.
  2. If companyName IS in DB but not showing in the edit form: the profile edit page is not loading it. Find the edit profile component and ensure useEffect fetches the founderProfile and sets companyName state from the API response.
  3. Manually fix Vikram's record: UPDATE "FounderProfile" SET "companyName"='DataMesh AI' WHERE "userId"=(SELECT id FROM "User" WHERE email='vikram.patel.qa2026@gmail.com')
VERIFY: Load the edit profile page for Vikram — Company Name field shows "DataMesh AI". Create a NEW founder via onboarding, enter a company name, then check the edit profile page — the name must appear.

=== BUG 4: ADD MOBILE HAMBURGER MENU (P1) ===
PROBLEM: The navigation has no mobile responsive behavior. Desktop nav items render at all viewport sizes, breaking on phones. The hamburger toggle button exists with class "md:hidden" but has no visible menu content when tapped.
FIND: grep -rn "Toggle menu" apps/frontend/ OR grep -rn "md:hidden" apps/frontend/src/components/ — find the main Nav/Header component
FIX STEPS:
  1. In the nav component, add useState for mobileOpen (boolean).
  2. The hamburger button (already exists as md:hidden) should toggle mobileOpen.
  3. When mobileOpen is true, render a full-width dropdown panel below the nav bar (md:hidden) with all nav links stacked vertically, each with min 44px tap targets (py-3).
  4. Include Log In + Get Started buttons in the mobile menu for unauthenticated users.
  5. Include Dashboard, Matches, Introductions, Chat, Profile, Settings, Sign Out for authenticated users.
  6. Each link click should setMobileOpen(false) to close the menu.
  7. Desktop nav (hidden md:flex) remains unchanged.
VERIFY: Open the app in a 375px viewport. Hamburger icon is visible. Tap it → menu slides down with all nav items. Tap a link → menu closes, page navigates. At 768px+ → hamburger is hidden, regular nav shows.

=== BUG 5: ADD ACTIVITY FEED TO DASHBOARD (P1) ===
PROBLEM: The dashboard has no activity feed. Users see matches and stats but no timeline of what happened.
FIND: 
  - Check if Activity model exists: grep -rn "model Activity" apps/backend/prisma/schema.prisma
  - Find the dashboard page: find apps/frontend -name "dashboard" -type d OR grep -rn "Dashboard" apps/frontend/src/app/
FIX STEPS:
  1. If Activity model doesn't exist in schema.prisma, add it:
     model Activity { id String @id @default(cuid()), userId String, user User @relation(fields:[userId],references:[id]), type String, title String, createdAt DateTime @default(now()) }
     Then run: npx prisma db push
  2. Create API endpoint GET /api/activities that returns the current user's latest 10 activities, ordered by createdAt desc.
  3. Add Activity creation calls at key events in the backend:
     - When a match is created: type "MATCH_FOUND", title includes the other user's name and company
     - When a user clicks Connect on a match: type "MATCH_CONNECTED"
     - When an introduction is generated: type "INTRO_SENT"
     - When an invite is used: type "INVITE_USED"
  4. Add an ActivityFeed component to the dashboard that fetches /api/activities and renders a timeline with emoji icons (🤝 match, ✅ connected, 📤 intro, 🎟️ invite) and relative timestamps ("5 min ago", "2 hours ago").
  5. Show "No activity yet" empty state with encouraging message.
VERIFY: Log in as any user. Dashboard shows "Recent Activity" section. If the user has matches, at least MATCH_FOUND activities should appear. If no activities exist yet, the empty state message appears cleanly.

=== BUG 6: MINOR FIXES (P2) ===

6a: FAKE STATS ON /about PAGE
FIND: grep -rn "12,000" apps/frontend/ OR grep -rn "5,000" apps/frontend/
FIX: Replace hardcoded "12,000+ Professionals", "94% Match Accuracy", "5,000+ Intros Made" with either real database counts fetched from /api/stats/public, OR qualitative text like "Growing community across India's startup hubs". Do NOT show fake numbers.
VERIFY: Visit /about — no obviously fake large numbers.

6b: NAV LINKS USE <a> INSTEAD OF <Link>
FIND: grep -rn '<a href=' apps/frontend/src/components/ — find navigation links not using Next.js Link
FIX: Replace all <a href="/path"> with <Link href="/path"> in navigation components. Import Link from 'next/link'.
VERIFY: Click any nav link — page navigates instantly via client-side routing (no full page reload, no white flash).

6c: HERO CHAT MOCKUP — 3RD CARD DISAPPEARS
FIND: Find the hero section component with the chat mockup (Ananya Bhat card, 88% match)
FIX: If the card uses a staggered CSS animation, add animation-fill-mode: forwards and ensure opacity starts at 1 (not 0) or the animation ends at opacity 1. Alternatively, remove the entrance animation on that card.
VERIFY: Reload homepage 5 times. The Ananya Bhat 88% match card is always visible.

=== IMPLEMENTATION ORDER ===
Do them in this exact order:
1. BUG 1 (cleanup bad matches + prevent future ones)
2. BUG 2 (regenerate reasoning)
3. BUG 3 (Vikram's company name)
4. BUG 6 (minor fixes — quick wins)
5. BUG 4 (mobile hamburger)
6. BUG 5 (activity feed)

=== FULL REGRESSION CHECKLIST (run after ALL fixes) ===
Test every item. If ANY fails, loop back and fix:
[ ] No same-persona matches exist in database
[ ] New match generation produces only cross-persona matches
[ ] All match reasoning is personalized (no generic placeholder text)
[ ] New matches get AI-generated reasoning (not hardcoded)
[ ] Vikram's edit profile shows "DataMesh AI" as company name
[ ] New founder onboarding saves and displays company name correctly
[ ] Mobile viewport (375px): hamburger icon visible, menu works
[ ] Desktop viewport (1024px+): regular nav visible, hamburger hidden
[ ] Dashboard shows activity feed section (even if empty state)
[ ] Activity records are created when matches are generated
[ ] /about page has no fake stats (12,000 / 5,000 removed)
[ ] Nav links use client-side routing (no full page reload)
[ ] Hero chat mockup: all 3 match cards always visible
[ ] Homepage still loads correctly
[ ] Login/signup modal still works
[ ] All existing pages (/features, /contact, /terms, /privacy) still load
[ ] No new console errors in browser
[ ] App builds successfully with no TypeScript errors
[ ] App deploys and runs at https://cleya.ai

After passing ALL items above, publish the app. You are done ONLY when every checkbox passes.

START NOW with BUG 1. Search the codebase first.